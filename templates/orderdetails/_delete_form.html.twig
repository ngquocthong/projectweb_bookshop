<form method="post" action="{{ path('app_orderdetails_delete', {'id': orderdetail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orderdetail.id) }}">
    <button class="btn btn-danger ms-2">Delete</button>
</form>
