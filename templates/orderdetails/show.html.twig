<!DOCTYPE html>

<title>Orderdetails</title>

{% block body %}
    <h1>Orderdetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ orderdetails.name }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_orderdetails_index') }}">back to list</a>

    <a href="{{ path('app_orderdetails_edit', {'id': orderdetail.id}) }}">edit</a>

    {{ include('orderdetails/_delete_form.html.twig') }}
{% endblock %}
